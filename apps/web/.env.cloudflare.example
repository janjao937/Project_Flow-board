# Build-time env for Cloudflare Free (OpenNext Workers) deploy
NEXT_PUBLIC_APP_URL=https://flowboard-web.<account>.workers.dev
# Leave empty — Join uses /runtime-config.json apiBaseUrl from KV
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_REQUIRE_RUNTIME_API=1
